[package]
name = "pxtone"
version = "0.1.0"
edition = "2024"

[dependencies]
byteorder = "1.5.0"
encoding_rs = "0.8.35"
lewton = "0.10.2"

[dev-dependencies]
toml = { version = "1.1.2", features = ["preserve_order"] }