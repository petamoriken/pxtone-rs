[package]
name = "lite-math"
version = "0.1.0"
edition = "2024"
publish = false
description = "The maths the decoder needs, from libm or from a compact series on wasm"
build = "build.rs"

[lib]
name = "lite_math"
